// 1. what is the first child of the table?

// 2a. what is the first child of a row in a table?
    // 2b. what's the next sibling of a row in a table?

// 3. what is the next sibling of a data element in the table?

// need a method "next child" to traverse from child to child
// Array.from(nodelist) converts a node list into an array.

function makeTable(rows, cols) {}
function makeButtons() {}
function addButtonFunctions() {} // add functions that the buttons will perform (using event listeners)
function addStyles() {}
function createAndAppend() {newElement, parent} // append newElement to parent.
function createAndAppendwText() {newElement, parent, text} // takes an Element and appends it to parent and gives it the appropriate text

